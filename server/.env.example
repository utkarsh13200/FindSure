PORT=4000
DATABASE_URL="file:./dev.db"
GOOGLE_MAPS_API_KEY=
DEMO_MODE=true

# --- Railway (set in the Railway dashboard; do not commit real values) ---
# PORT                 → provided automatically by Railway
# DATABASE_URL         → file:./data/prod.db   (use a Railway Volume mounted at /app/data for persistence)
# DEMO_MODE            → true
# NODE_ENV             → production
# GOOGLE_MAPS_API_KEY  → optional; leave empty to keep demo Places fallback
